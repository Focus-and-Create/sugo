package com.seogoapp.ui.theme

import androidx.compose.ui.graphics.Color

// ── 노션 스타일 단색 미니멀 팔레트 ──

val NotionBackground   = Color(0xFFFFFFFF)   // 순백 배경
val NotionSurface      = Color(0xFFF7F7F5)   // 카드·서피스
val NotionBorder       = Color(0xFFE9E9E7)   // 구분선
val NotionText         = Color(0xFF37352F)   // 기본 텍스트
val NotionTextSub      = Color(0xFF787774)   // 보조 텍스트
val NotionTextHint     = Color(0xFFACACA8)   // 힌트 텍스트

// 액센트 — 서고 특유의 부드러운 세피아·인디고 계열
val SeogoAccent        = Color(0xFF4F6EF7)   // 인디고 포인트 (FAB, 링크)
val SeogoAccentMuted   = Color(0xFFEEF1FD)   // 연한 인디고 배경 (칩)
val SeogoCharacterChip = Color(0xFFF0EDEA)   // 캐릭터 태그 배경 (크림)

// 다크 모드 대응 (Phase 4)
val NotionBackgroundDark = Color(0xFF191919)
val NotionSurfaceDark    = Color(0xFF252525)
val NotionTextDark       = Color(0xFFEAEAE7)
val NotionTextSubDark    = Color(0xFF9A9A97)
